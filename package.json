{
  "name": "meal-image-bot",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@slack/bolt": "^4.6.0",
    "cheerio": "^1.2.0"
  },
  "scripts": {
    "dev": "NODE_ENV=development bun run --watch index.ts",
    "start": "NODE_ENV=production bun run index.ts"
  }
}
